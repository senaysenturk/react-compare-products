import React from "react";
import style from "./style.module.scss";

const Home = () => {
  return (
    <>
      <div className={`${style["header"]}`}>Home</div>
    </>
  );
};

export default Home;
